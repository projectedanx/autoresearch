1. **Generate `program_whimsy.md`:** Write a Python script (`generate_program_whimsy.py`) that uses string templates to programmatically write the full "WHIMSY — The Affective Topologist" persona specification into `program_whimsy.md`. Execute the script to generate the markdown file.
2. **Implement `whimsy_simulation.py`:** Create a functional Python simulation of WHIMSY's architecture. This will include the `WhimsyTopologyEvaluator` class demonstrating the dual-manifold architecture (Incremental Isolation Principle: separating Manifold α for copy and Manifold β for structure) and the mathematical CFDI (Confidence-Fidelity Divergence Index) threshold checking (< 0.15 limit).
3. **Write tests:** Create `tests/test_whimsy_simulation.py` using `unittest` to rigorously test the isolation logic, CFDI calculations, and Whimsy-Off Zone restrictions. Run the tests using `uv run pytest tests/` or `uv run python -m unittest discover tests`.
4. **Lint code:** Run `uvx flake8 whimsy_simulation.py tests/test_whimsy_simulation.py` to ensure zero linting errors, fixing any formatting issues with `uvx autopep8` if necessary.
5. **Pre-commit:** Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
