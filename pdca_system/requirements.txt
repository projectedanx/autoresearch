# PDCA system only (dashboard + daemon + workflow + run_arxiv). Install in host project when copying pdca_system.
# Audit: see pyproject.toml; only these are imported by pdca_system (no torch, kernels, etc.).
fastapi>=0.116.0
uvicorn>=0.35.0
pydantic>=2.0
jinja2>=3.1.0
python-multipart>=0.0.20
arxiv>=2.4.1
matplotlib>=3.8.0
pandas>=2.2.0

# Optional: for running tests (FastAPI TestClient). Omit for production.
# httpx>=0.28.0
