# Formal Mathematical Specification: The Causal Path Integrity (CPI) Constraint

The **Temporal Blending Engine (TBE)** is a multi-agent orchestration architecture designed to resolve **Chronotopological Drift** when fusing temporally divergent conceptual spaces under the governance of the **Verifiable Cognition Stack (VCS)**. To translate the high-level system requirements of the TBE into a machine-enforceable, programmatic contract, we construct an **isomorphic formalization** of the **Causal Path Integrity (CPI)** metric.

## 1. Isomorphic Formalization of the Temporal Blending State-Space

To formally model **Epistemic Rheology**—the continuous flow and deformation of semantic concepts under temporal and logical constraints—we define a hybrid state-space model that maps continuous semantic trajectories to discrete causal transitions.

### Definition 1.1: The Blended Latent Space
Let $M$ be a high-dimensional Riemannian manifold representing the generative model's latent space. A Double-Scope Blend (DSCB) constructs a continuous trajectory $S_t \in M$ parameterized by the virtual timeline $t \in [0, T]$. This trajectory is governed by the mapping:

$$S_t = \Phi(X_N(t), X_C(t), W_t)$$

where $X_N(t)$ and $X_C(t)$ are the semantic trajectories of the input spaces, and $W_t$ is the dynamic weighting tensor.

### Definition 1.2: The Discrete Causal State
The continuous trajectory $S_t$ is projected onto a discrete set of world states via a quantization operator $Q$. A discrete state $s_k$ is defined by a valuation vector over a finite set of Boolean fluents $F = \{f_1, f_2, \dots, f_m\}$.

### Definition 1.3: Causal Actions and World Rules
An action $a$ is defined by its Preconditions and Effects. The Frame Operator ensures that variables not modified by the action remain invariant.

## 2. Mathematical Definition of Causal Path Integrity (CPI)

The CPI score quantifies logical adherence to the laws of cause-and-effect over a discrete trace of $N$ states generated by the probabilistic LLM.

$$CPI(\tau) = \frac{1}{N-1} \sum_{k=1}^{N-1} I( s_k \models Pre(a_k) \wedge s_{k+1} \models Eff(a_k) \wedge Frame(s_k, s_{k+1}, a_k) )$$

The VCS enforces a threshold of $CPI(\tau) \ge 0.95$.

## 3. The Mathematical Proofs

### Theorem 3.1: The Cascading Contradiction Boundary
If the SAA enforces $CPI(\tau) \ge 0.95$, the probability that a subsequent action executes under a false assumption is strictly bounded. For any sequence length $N < 21$, a single causal contradiction is mathematically impossible to pass.

### Theorem 3.2: Epistemic Rheological Stability
Modeling semantic flow as a viscous fluid prevents sudden, discontinuous jumps (Chronotopological Drift), bounded by the Lipschitz constant $L = ||f_{constraint}|| / \mu$.

## 4. Parametric Trade-off Modeling

The tension frontier maps Coherence (CCH) against Novelty (CSD), requiring that $CSD \le Budget_{threshold}$.
